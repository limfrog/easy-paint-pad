# 🎨 Easy Paint Pad 사용설명서

> 기준 버전: **v1.6.32**  
> 이 문서는 현재 프로그램의 실제 UI와 기능을 기준으로 작성했습니다.

## 목차

1. [빠른 시작](#1-빠른-시작)
2. [화면 구성](#2-화면-구성)
3. [레이어](#3-레이어)
4. [객체 관리](#4-객체-관리)
5. [영역 선택](#5-영역-선택)
6. [그리기 도구](#6-그리기-도구)
7. [도형](#7-도형)
8. [텍스트](#8-텍스트)
9. [악보/기호 도형](#9-악보기호-도형)
10. [색상과 투명도](#10-색상과-투명도)
11. [파일 관리](#11-파일-관리)
12. [크기조절과 문서화](#12-크기조절과-문서화)
13. [확대/축소와 화면 이동](#13-확대축소와-화면-이동)
14. [모바일 사용](#14-모바일-사용)
15. [단축키 전체](#15-단축키-전체)
16. [EPP 프로젝트](#16-epp-프로젝트)
17. [PWA와 업데이트](#17-pwa와-업데이트)
18. [사용 팁](#18-사용-팁)

---

# 1. 빠른 시작

Easy Paint Pad의 기본 작업 순서는 다음과 같습니다.

1. **파일 → 새 파일** 또는 **열기**로 작업을 시작합니다.
2. 오른쪽 **레이어 패널**에서 작업할 레이어를 선택합니다.
3. **그리기** 탭에서 도구를 선택합니다.
4. 캔버스에 그림, 도형, 텍스트 또는 이미지를 배치합니다.
5. **객체 선택(V)**으로 개별 객체를 수정합니다.
6. 필요하면 레이어를 추가하거나 객체를 다른 레이어로 옮깁니다.
7. 계속 수정할 작업은 **EPP 저장**으로 저장합니다.
8. 완성 결과는 PNG/JPEG/WebP/PDF로 내보냅니다.

---

# 2. 화면 구성

## 상단 탭

### 파일

파일과 캔버스 전체 작업을 담당합니다.

- 새 파일
- 최근 파일
- 열기
- EPP 저장
- PNG 저장
- 다른 이름으로 저장
- 인쇄
- PDF 저장
- 크기조절
- 문서화
- 캔버스 너비/높이 변경

### 그리기

실제 편집 작업을 담당합니다.

맨 앞에 **실행취소 / 다시실행**이 있고 그 뒤에 다음 기능이 배치됩니다.

- 영역 선택
- 객체 선택/이동
- 연필
- 브러시
- 지우개
- 채우기
- 스포이드
- 텍스트
- 도형/기호
- 선 / 사각형 / 타원 / 화살표 / 오선
- 선 두께
- 텍스트 옵션
- 색상 / 팔레트 / 투명도

### 보기

- 축소
- 확대
- 100%
- 화면 맞추기
- 배율 슬라이더
- 라이트 / 다크 테마

### 도움말

- 프로그램 정보
- 내장 사용설명서
- 제작자 연락

## 오른쪽 패널

오른쪽 패널은 두 부분으로 나뉩니다.

1. **레이어**
2. **현재 레이어 객체**

데스크톱에서는 항상 오른쪽에 표시되고, 모바일에서는 상단 레이어 버튼을 눌러 오버레이 패널로 엽니다.

---

# 3. 레이어

Easy Paint Pad의 레이어는 단순한 객체 순서가 아니라 독립적으로 관리되는 캔버스 레이어입니다.

각 객체는 `layerId`를 가지고 하나의 레이어에 소속됩니다.

## 새 레이어 만들기

레이어 패널의 `＋` 버튼을 누르면 새 레이어가 생성되고 새 레이어가 현재 레이어가 됩니다.

이후 새로 만드는 객체는 현재 레이어에 배치됩니다.

## 레이어 선택

레이어 목록에서 원하는 레이어를 클릭합니다.

선택된 레이어가 **현재 레이어**가 되며, 아래의 객체 목록에는 해당 레이어의 객체만 표시됩니다.

## 레이어 순서

`↑ / ↓` 버튼으로 현재 레이어의 순서를 변경할 수 있습니다.

레이어 목록에서 위쪽에 표시되는 레이어가 화면에서도 더 위쪽에 렌더링됩니다.

## 레이어 표시 / 숨김

레이어 행의 표시 버튼:

- `◉` — 보이는 상태
- `○` — 숨겨진 상태

레이어를 숨기면:

- 그 레이어의 모든 객체가 캔버스에서 보이지 않습니다.
- 그 레이어의 객체는 선택 대상에서도 제외됩니다.
- 다시 표시하면 원래 위치 그대로 나타납니다.

## 레이어 잠금

레이어 행의 자물쇠 버튼:

- `🔓` — 편집 가능
- `🔒` — 잠금

잠긴 레이어의 객체는:

- 캔버스 클릭으로 선택되지 않습니다.
- 이동할 수 없습니다.
- 크기를 바꿀 수 없습니다.
- 삭제할 수 없습니다.
- 텍스트 편집을 할 수 없습니다.
- 다중 선택 대상에서 제외됩니다.
- 영역 선택 편집 대상에서 제외됩니다.

레이어를 잠그는 순간 해당 레이어의 객체가 선택돼 있었다면 선택도 해제됩니다.

## 레이어 이름 바꾸기

레이어 이름을 **더블클릭**하면 이름을 변경할 수 있습니다.

## 레이어 삭제

`✕` 버튼으로 현재 레이어를 삭제할 수 있습니다.

레이어 안에 객체가 있으면 삭제 확인 창이 표시됩니다.  
레이어는 최소 1개가 유지됩니다.

## 객체를 다른 레이어로 이동

1. 객체를 하나 또는 여러 개 선택합니다.
2. 객체 목록 상단의 **이동할 레이어**에서 대상 레이어를 고릅니다.
3. **이동** 버튼을 누릅니다.

주의:

- 잠긴 원본 레이어의 객체는 이동할 수 없습니다.
- 잠긴 대상 레이어로는 이동할 수 없습니다.
- 숨겨진 레이어로 이동하는 것은 가능합니다.
- 숨겨진 레이어로 옮긴 객체는 즉시 화면에서 사라지고, 해당 레이어를 다시 표시하면 나타납니다.

---

# 4. 객체 관리

Easy Paint Pad는 다음 항목을 객체로 관리합니다.

- 이미지
- 자유 획
- 도형
- 텍스트
- 악보/기호 도형

## 객체 선택

**객체 선택/이동(V)** 도구를 사용합니다.

- 클릭 — 객체 선택
- 드래그 — 객체 이동
- 선택 핸들 드래그 — 크기 변경
- 빈 곳 클릭 — 선택 해제
- 텍스트 더블클릭 — 텍스트 편집
- `Shift + 클릭` — 다중 선택 추가/제거

## 빈 공간 드래그 다중 선택

객체 선택 도구에서 빈 공간을 드래그하면 선택 사각형이 만들어집니다.

선택 범위와 겹치는 편집 가능한 객체를 여러 개 선택할 수 있습니다.

잠긴 레이어의 객체는 선택되지 않습니다.

## 여러 객체 이동

여러 객체가 선택된 상태에서 그중 하나를 드래그하면 선택된 객체를 함께 이동합니다.

## 객체 개별 잠금

레이어 전체 잠금과 별도로 객체 하나만 잠글 수도 있습니다.

객체 목록의 자물쇠 버튼을 사용합니다.

- 객체 잠금은 해당 객체의 이동/수정을 제한합니다.
- 레이어가 잠겨 있으면 개별 객체 잠금 버튼도 사용할 수 없습니다.
- 레이어 잠금이 개별 객체 잠금보다 우선합니다.

## 객체 순서

같은 레이어 안에서 선택 객체의 앞뒤 순서를 바꿀 수 있습니다.

- `Ctrl+]` / `⌘+]` — 위로
- `Ctrl+[` / `⌘+[` — 아래로
- 객체 목록 상단의 `↑ / ↓`

이 기능은 **레이어 자체를 이동하는 기능과 다릅니다.**

- 레이어 패널 `↑ / ↓` → 레이어 순서
- 객체 패널 `↑ / ↓` → 현재 레이어 안의 객체 순서

## 객체 복제

`Ctrl+D` / `⌘+D`

선택 객체를 복제합니다.

## Ctrl/Cmd + 드래그

객체 선택 도구에서 `Ctrl` 또는 `⌘`을 누른 채 드래그하면 복제 이동으로 사용할 수 있습니다.

## 화살표 이동

선택 객체:

- 방향키 — 1px 이동
- `Shift + 방향키` — 10px 이동

---

# 5. 영역 선택

**영역 선택(R)**은 객체가 아니라 픽셀 영역을 다루는 도구입니다.

객체 선택 도구(V)와 용도가 다릅니다.

## 기본 사용

1. 영역 선택 도구를 선택합니다.
2. 캔버스를 드래그하여 점선 사각형을 만듭니다.
3. 선택 영역 안을 드래그하면 픽셀이 분리되어 이동합니다.
4. 원하는 위치로 옮깁니다.
5. 캔버스의 다른 곳을 클릭하여 확정합니다.

## 현재 레이어 기준

범위 선택 작업은 시작한 레이어에 고정됩니다.

예:

- 레이어 1이 잠겨 있음
- 레이어 2가 현재 레이어이며 잠금 해제 상태
- 레이어 2에서 범위를 지정

이 경우 범위 선택과 이동은 레이어 2 기준으로 처리되며 잠긴 레이어 1의 픽셀/객체는 편집 대상에 포함되지 않습니다.

## 잠긴 레이어

잠긴 레이어에서는 범위 선택 편집을 할 수 없습니다.

잠금 상태의 다른 레이어는 현재 레이어의 범위 선택을 방해하지 않으며, 그 내용은 범위 복사/잘라내기 대상에서 제외됩니다.

## 영역 선택 조작

| 동작 | 결과 |
|---|---|
| 드래그 | 범위 지정 |
| 범위 안 드래그 | 선택 픽셀 이동 |
| `Ctrl/Cmd + 드래그` | 복사 이동 |
| `Ctrl/Cmd + C` | 범위 복사 |
| `Ctrl/Cmd + X` | 범위 잘라내기 |
| `Ctrl/Cmd + V` | 픽셀 붙여넣기 |
| `Delete` / `Backspace` | 범위 삭제 |
| `Esc` | 이동 취소 / 선택 해제 |
| 방향키 | 1px 이동 |
| `Shift + 방향키` | 10px 이동 |

## 클립보드 이미지

외부 앱에서 복사한 이미지를 붙여넣을 수 있습니다.

- 영역 선택 도구가 활성 상태면 이동 가능한 픽셀 선택으로 들어옵니다.
- 다른 도구에서는 이미지 객체로 삽입됩니다.

---

# 6. 그리기 도구

## 연필 (`P`)

기본 자유선 도구입니다.

현재 전경색, 전경 투명도, 선 두께를 사용합니다.

## 브러시 (`B`)

연필보다 굵은 자유선입니다.

현재 선 두께 값의 약 3배 굵기로 만들어집니다.

## 지우개 (`E`)

획 형태의 지우개입니다.

현재 레이어에 지우개 스트로크가 생성됩니다.

## 채우기 (`F`)

클릭한 영역을 현재 전경색으로 채웁니다.

## 스포이드 (`K`)

캔버스의 색을 읽어서 전경색으로 설정합니다.

## 선 두께

기본 프리셋:

- 1px
- 3px
- 5px
- 8px

`− / ＋` 또는 숫자 입력 UI를 이용해 직접 조절할 수 있습니다.

---

# 7. 도형

지원 도형:

- 선
- 사각형
- 타원
- 화살표
- 오선

## 채우기

도형 채우기:

- 없음
- 전경
- 배경

## 선 각도

선 도구에서는 각도 옵션을 사용할 수 있습니다.

- 자유
- 수평 0°
- 수직 90°
- 대각 45°
- 대각 135°
- Shift 고정

## 오선

악보 작성용 5선 도형입니다.

오선도 일반 도형 객체이므로 선택 도구로 이동/리사이즈할 수 있습니다.

---

# 8. 텍스트

## 새 텍스트 입력

1. 텍스트 도구 `T`를 선택합니다.
2. 캔버스를 클릭합니다.
3. 텍스트를 입력합니다.
4. `Esc` 또는 다른 위치 클릭으로 확정합니다.

## 기존 텍스트 편집

객체 선택 도구에서 텍스트를 더블클릭합니다.

오른쪽 객체 목록에서도 텍스트 객체를 더블클릭하여 편집할 수 있습니다.

잠긴 객체 또는 잠긴 레이어의 텍스트는 편집할 수 없습니다.

## 텍스트 스타일

- 폰트
- 크기
- 굵게
- 기울임
- 밑줄
- 전경색
- 투명도

## 박스 텍스트

`□` 버튼으로 박스 텍스트를 사용할 수 있습니다.

설정:

- 패딩
- 모서리 반경
- 테두리 두께
- 배경 사용 여부
- 배경색
- 배경 투명도

## 웹폰트

한글/영문 폰트를 여러 종류 제공합니다.

예:

- Noto Sans KR
- Nanum Gothic
- Black Han Sans
- Do Hyeon
- Jua
- Gowun Dodum
- Nanum Myeongjo
- Noto Serif KR
- Gowun Batang
- Playfair Display
- Merriweather
- Lora
- EB Garamond
- Cormorant Garamond
- Raleway
- Oswald 등

일부 웹폰트는 인터넷 연결이 필요할 수 있습니다.

---

# 9. 악보/기호 도형

그리기 탭의 **도형(M)** 도구는 문자 기반 기호를 객체로 삽입합니다.

현재 제공되는 주요 기호:

- 높은음자리표
- 낮은음자리표
- 가온음자리표
- 내림표
- 올림표
- 제자리표
- 세뇨
- 코다
- 페르마타
- 4분쉼표
- 8분쉼표
- 16분쉼표
- 4분음표
- 8분음표
- 16분음표
- 도돌이표 시작
- 도돌이표 끝
- 겹세로줄
- Final 겹세로줄
- 세로줄

## 삽입 방법

### 캔버스 클릭

1. `M` 또는 도형 도구를 선택합니다.
2. 목록에서 기호를 고릅니다.
3. 캔버스를 클릭합니다.

### 중앙 삽입

**중앙삽입** 버튼을 누르면 캔버스 중앙에 배치됩니다.

### 직접 입력

직접 입력 칸에 원하는 문자/기호를 넣어 삽입할 수 있습니다.

## 크기

악보/기호 도형은 고정된 별도 기본 크기를 사용하지 않고 **현재 텍스트 도구의 글자 크기**를 따라갑니다.

삽입 후에는 일반 텍스트 계열 객체처럼 선택, 이동, 크기 변경이 가능합니다.

Noto Music 웹폰트를 사용하므로 일부 기호는 폰트 로딩 전에 대체문자로 잠시 보일 수 있습니다.

---

# 10. 색상과 투명도

## 전경색

주로 다음 항목에 사용됩니다.

- 연필
- 브러시
- 선
- 도형 테두리
- 텍스트
- 기호 도형

## 배경색

주로 다음 항목에 사용됩니다.

- 도형 배경 채우기
- 박스 텍스트 배경

## 기본 팔레트

그리기 탭의 팔레트에서 바로 색상을 선택할 수 있습니다.

## 사용자 색상 슬롯

4개의 커스텀 색상 슬롯이 있습니다.

데스크톱 기준:

- 좌클릭 — 전경색으로 적용
- 더블클릭 — 슬롯 색상 편집
- 우클릭 — 배경색으로 적용

모바일에서는 길게 누르기 등 터치용 동작이 제공됩니다.

## 투명도

전경과 배경을 각각 0~100%로 조절할 수 있습니다.

## 모바일 색상 UI

모바일에서는 겹쳐진 전경/배경 색상 박스 대신 구분된 전경색/배경색 버튼을 표시합니다.

---

# 11. 파일 관리

## 새 파일

기본 캔버스:

```text
800 × 600 px
```

새 파일을 만들 때 기존 작업이 저장되지 않았다면 저장 여부를 확인합니다.

## 열기

지원:

- 일반 이미지
- EPP 프로젝트

이미지를 새 파일로 열면 해당 이미지 크기로 캔버스가 설정됩니다.

## 드래그앤드롭

이미지를 캔버스 영역으로 드래그할 수 있습니다.

- 빈 캔버스에 드롭 → 새 파일처럼 열기
- 작업 중 일반 드롭 → 현재 캔버스에 이미지 객체로 삽입
- `Shift + 드롭` → 새 파일로 열기
- EPP 파일 드롭 → 프로젝트 열기

현재 캔버스에 삽입되는 큰 이미지는 캔버스 안에 들어오도록 자동 축소될 수 있습니다.

## EPP 저장

`Ctrl+S` / `⌘+S`

프로젝트 상태를 보존합니다.

자세한 내용은 [EPP 프로젝트](#16-epp-프로젝트) 참고.

## PNG 저장

현재 보이는 레이어들을 합쳐 PNG로 저장합니다.

## 다른 이름으로 저장

지원:

- PNG
- JPEG
- WebP

JPEG/WebP에서는 품질을 조절할 수 있습니다.

## PDF 저장

A4 세로 페이지에 현재 결과를 비율 유지하여 배치합니다.

- 가로 중앙 정렬
- 세로 상단 배치
- 파일명은 현재 문서명을 기준으로 생성

PDF 저장 시 jsPDF 라이브러리를 CDN에서 불러오므로 인터넷 연결이 필요할 수 있습니다.

## 인쇄

현재 캔버스를 이미지로 합쳐 별도의 인쇄 창에서 출력합니다.

## 최근 파일

지원 브라우저에서는 EPP 또는 이미지 저장 시 파일 핸들을 IndexedDB에 기록해 최근 작업 파일로 다시 접근할 수 있습니다.

- 최대 10개
- 실제 파일을 삭제하지 않고 최근 목록에서만 제거 가능
- 브라우저가 File System Access API를 지원해야 함
- 앱 UI에서는 Chrome 또는 Edge 사용을 안내

---

# 12. 크기조절과 문서화

## 이미지 크기조절

파일 탭의 **크기조절** 기능입니다.

범위:

```text
10% ~ 400%
```

현재 전체 결과를 한 번 평탄화(flatten)한 뒤 새 크기로 변환합니다.

따라서 크기조절을 실행하면 기존의 여러 객체를 그대로 유지하는 리사이즈가 아니라 **전체 결과가 하나의 이미지 객체로 합쳐지는 방식**입니다.

계속 객체별 편집이 필요한 경우 크기조절 전에 EPP 파일을 별도로 저장해 두는 것이 좋습니다.

## 캔버스 크기

파일 탭에서 너비/높이를 직접 조절하고 적용할 수 있습니다.

캔버스 크기 변경은 이미지 전체 비율 변경과 다른 기능입니다.

## 문서화

사진으로 찍은 문서의 음영을 줄이고 배경을 밝게 만드는 기능입니다.

과정:

1. 파일 탭 → **문서화**
2. 강도 슬라이더 조절
3. 미리보기 확인
4. 적용

내부적으로 주변 밝기를 추정하여 음영을 보정하고 문서 형태에 가깝게 변환합니다.

문서화 결과는 래스터 이미지로 적용되므로 중요한 원본은 먼저 EPP로 저장하는 것을 권장합니다.

---

# 13. 확대축소와 화면 이동

## 보기 탭

- 축소
- 확대
- 100%
- 맞추기
- 10~800% 배율 슬라이더

## 마우스 휠

`Ctrl/Cmd + 휠`

확대/축소합니다.

## Space + 드래그

`Space`를 누른 상태에서 캔버스를 드래그하면 작업영역 안에서 캔버스 위치를 이동할 수 있습니다.

이 기능은 객체를 움직이는 것이 아니라 화면상의 캔버스 배치 위치만 이동합니다.

## 테마

- 라이트
- 다크

선택한 테마는 브라우저 저장소에 기억됩니다.

---

# 14. 모바일 사용

Easy Paint Pad에는 모바일용 별도 동작이 포함되어 있습니다.

## 레이어 패널

상단의 레이어 아이콘을 누르면 오른쪽 패널이 화면 위로 열립니다.

바깥 영역을 누르면 닫힙니다.

## 한 손가락

현재 도구에 따라:

- 그리기
- 선택
- 객체 이동
- 범위 선택
- 텍스트 위치 지정

등을 수행합니다.

## 두 손가락 핀치

캔버스 확대/축소를 합니다.

핀치 중심점이 크게 움직이지 않도록 스크롤 위치도 함께 보정됩니다.

## 모바일 객체 드래그

객체를 선택한 뒤 바로 드래그할 수 있도록 터치 이동과 더블탭을 구분합니다.

실제로 움직이지 않고 짧게 두 번 누른 경우만 더블탭으로 처리합니다.

## 모바일 텍스트

가상 키보드가 열리면서 화면 높이가 바뀌면 텍스트 입력창이 보이도록 위치를 보정합니다.

## 모바일 색상

전경색과 배경색을 각각 구분된 버튼으로 표시하며 터치용 컬러 선택 창을 사용합니다.

---

# 15. 단축키 전체

## 파일

| 기능 | Windows / Linux | macOS |
|---|---|---|
| 새 파일 | `Ctrl+N` | `⌘+N` |
| 열기 | `Ctrl+O` | `⌘+O` |
| EPP 저장 | `Ctrl+S` | `⌘+S` |
| EPP 저장 | `Ctrl+E` | `⌘+E` |
| 다른 이름으로 저장 | `Ctrl+Shift+S` | `⌘+Shift+S` |
| 인쇄 | `Ctrl+P` | `⌘+P` |

## 실행취소 / 다시실행

| 기능 | Windows / Linux | macOS |
|---|---|---|
| 실행취소 | `Ctrl+Z` | `⌘+Z` |
| 다시실행 | `Ctrl+Y` | `⌘+Y` |
| 다시실행 | `Ctrl+Shift+Z` | `⌘+Shift+Z` |

실행취소 이력은 최대 60단계로 관리됩니다.

## 도구

| 키 | 도구 |
|---|---|
| `R` | 영역 선택 |
| `V` | 객체 선택/이동 |
| `P` | 연필 |
| `B` | 브러시 |
| `E` | 지우개 |
| `F` | 채우기 |
| `K` | 스포이드 |
| `T` | 텍스트 |
| `M` | 도형/기호 |

## 편집

| 기능 | Windows / Linux | macOS |
|---|---|---|
| 복사 | `Ctrl+C` | `⌘+C` |
| 붙여넣기 | `Ctrl+V` | `⌘+V` |
| 범위 잘라내기 | `Ctrl+X` | `⌘+X` |
| 복제 | `Ctrl+D` | `⌘+D` |
| 전체 선택 | `Ctrl+A` | `⌘+A` |
| 객체 위로 | `Ctrl+]` | `⌘+]` |
| 객체 아래로 | `Ctrl+[` | `⌘+[` |
| 삭제 | `Delete` / `Backspace` | `Delete` |
| 취소/선택 해제 | `Esc` | `Esc` |

## 확대/축소

| 기능 | 키 |
|---|---|
| 확대 | `+` 또는 `=` |
| 축소 | `-` |
| 화면상 캔버스 이동 | `Space + 드래그` |

## 선택 객체 이동

| 기능 | 키 |
|---|---|
| 1px 이동 | 방향키 |
| 10px 이동 | `Shift + 방향키` |

## 텍스트 편집 중

- `Esc` — 텍스트 확정
- `Ctrl/Cmd + B` — 굵게
- `Ctrl/Cmd + I` — 기울임

---

# 16. EPP 프로젝트

EPP는 Easy Paint Pad의 편집용 프로젝트 형식입니다.

일반 이미지와 달리 다시 열었을 때 객체와 레이어를 계속 편집할 수 있습니다.

## 저장되는 정보

- 프로젝트 버전
- 캔버스 너비/높이
- 모든 객체
- 이미지 원본 데이터
- 획 좌표
- 도형 속성
- 텍스트와 글꼴/스타일
- 객체별 레이어 ID
- 레이어 목록
- 레이어 순서
- 레이어 이름
- 레이어 표시 상태
- 레이어 잠금 상태
- 현재 활성 레이어
- 프로젝트 제목
- 저장 시각

## 이전 EPP 호환

레이어 정보가 없는 이전 프로젝트를 열면 기본 레이어가 생성되고 기존 객체들이 그 레이어에 배치되도록 처리됩니다.

## 권장 저장 방식

작업 중:

```text
EPP
```

최종 결과 공유:

```text
PNG / JPEG / WebP / PDF
```

두 형식을 함께 보관하면 안전합니다.

---

# 17. PWA와 업데이트

Easy Paint Pad는 `manifest.json`과 `sw.js`를 이용한 PWA 구조를 사용합니다.

## 서비스워커

서비스워커는 다음 파일을 캐시합니다.

- 앱 시작 경로
- `index.html`
- `manifest.json`
- 앱 아이콘

HTML과 manifest는 네트워크에서 최신 버전을 먼저 확인하고, 연결되지 않을 때 캐시를 사용합니다.

## 새 버전 감지

새 서비스워커가 설치되면 앱에서 업데이트 안내 배너를 표시합니다.

사용자가 업데이트를 선택하면 새 서비스워커가 활성화되고 페이지가 다시 로드됩니다.

## 일반 버전 업데이트

현재 프로젝트 관리 방식에서는 일반 기능 업데이트 때:

```text
index.html
sw.js
```

두 파일을 교체합니다.

`manifest.json`은 앱 이름, 아이콘, 시작 경로 등 PWA 메타데이터 자체가 변경될 때만 수정합니다.

---

# 18. 사용 팁

## 작업 중에는 EPP 저장

PNG/JPEG/WebP는 완성 이미지이므로 객체와 레이어 정보가 사라집니다.

수정 가능 상태를 유지하려면 EPP를 함께 저장하세요.

## 레이어를 역할별로 나누기

예:

```text
레이어 4  글자/주석
레이어 3  기호/도형
레이어 2  직접 그린 내용
레이어 1  원본 이미지
```

원본 이미지 레이어를 잠그고 위 레이어에서 편집하면 실수로 배경을 움직이는 일을 줄일 수 있습니다.

## 객체와 범위 선택을 구분하기

- 객체 자체를 옮기거나 수정 → **객체 선택(V)**
- 이미지 일부의 픽셀을 잘라서 옮김 → **영역 선택(R)**

## 잠금의 두 단계

- **레이어 잠금** — 레이어 전체 보호
- **객체 잠금** — 특정 객체만 보호

레이어 잠금이 더 강한 제한입니다.

## 확대해서 정밀 편집

보기 탭에서 확대하거나 두 손가락 핀치를 사용한 뒤 작업하면 작은 객체를 다루기 쉽습니다.

## 문서화/크기조절 전 백업

문서화와 전체 이미지 크기조절은 래스터 이미지 중심의 처리이므로 원본 객체 구조가 필요한 경우 먼저 EPP 저장을 권장합니다.

---

문서가 실제 프로그램과 달라진 부분을 발견하면 프로그램 버전과 함께 문서를 업데이트해 주세요.

**Easy Paint Pad v1.6.32**
