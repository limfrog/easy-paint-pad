# 🎨 Easy Paint Pad 사용설명서

> 브라우저에서 바로 사용하는 그림판 앱 · 설치 불필요 · 단일 HTML 파일

---

## 목차

1. [화면 구성](#화면-구성)
2. [파일 관리](#파일-관리)
3. [도구 사용법](#도구-사용법)
4. [도형 그리기](#도형-그리기)
5. [텍스트 입력](#텍스트-입력)
6. [색상 및 팔레트](#색상-및-팔레트)
7. [객체 선택 및 편집](#객체-선택-및-편집)
8. [영역 선택](#영역-선택)
9. [캔버스 크기 조절](#캔버스-크기-조절)
10. [보기 및 테마](#보기-및-테마)
11. [전체 단축키](#전체-단축키)

---

## 화면 구성

```
┌─────────────────────────────────────────────┐
│  타이틀바: 파일명 · Easy Paint Pad           │
├─────────────────────────────────────────────┤
│  탭바: [파일] [그리기] [보기]                │
├─────────────────────────────────────────────┤
│  리본: 탭별 도구 옵션                        │
├──────────────────────┬──────────────────────┤
│                      │  객체 목록 패널       │
│      캔버스          │  (우측)               │
│                      │  · 객체 순서 표시     │
│                      │  · 클릭: 선택         │
│                      │  · 더블클릭: 편집(텍스트)│
│                      │  · ✕: 삭제            │
├──────────────────────┴──────────────────────┤
│  상태바: 도구 · 좌표 · 캔버스 크기          │
└─────────────────────────────────────────────┘
```

---

## 파일 관리

### 파일 형식

| 형식 | 확장자 | 설명 |
|---|---|---|
| 그림 저장 | `.png` `.jpg` `.webp` | 픽셀 이미지 (편집 불가) |
| 프로젝트 저장 | `.epp` | 모든 객체 정보 유지 (편집 가능) |
| PDF 저장 | `.pdf` | A4 크기로 출력용 저장 |

### 파일 작업

- **새 파일** — 현재 작업을 닫고 빈 캔버스 시작 (미저장 시 저장 여부 확인)
- **열기** — 이미지 파일(PNG·JPG 등) 또는 EPP 프로젝트 파일 열기
- **저장** — 현재 파일명으로 PNG 저장
- **다른이름저장** — 파일명·형식 선택 후 저장
- **EPP 저장** — 프로젝트 파일로 저장 (객체·스타일 모두 보존)
- **PDF 저장** — A4 크기 PDF로 저장 (비율 유지, 중앙 배치)
- **인쇄** — 브라우저 인쇄 대화상자 열기
- **크기조절** — 전체 이미지를 비율 유지하며 %-단위로 리사이즈

### 이미지 불러오기

- **파일 버튼** — 파일 선택창에서 이미지 또는 EPP 선택
- **드래그앤드롭** — 이미지 파일을 캔버스에 드래그
  - 일반 드롭 → 현재 캔버스에 객체로 삽입
  - **Shift + 드롭** → 새 파일로 열기
- **클립보드 붙여넣기** — 다른 앱에서 복사한 이미지를 Ctrl+V로 삽입

### 미저장 알림

파일을 수정하면 타이틀바에 **● 표시**가 나타납니다.  
새 파일 열기 또는 브라우저 종료 시 저장 여부를 확인합니다.  
저장 확인창에서 **그림 저장** 또는 **EPP 저장** 중 선택할 수 있습니다.

---

## 도구 사용법

### 그리기 도구

| 도구 | 단축키 | 설명 |
|---|---|---|
| 영역 선택 | `R` | 픽셀 단위 영역 선택·이동·복사 |
| 객체 선택 | `V` | 객체 클릭 선택, 이동, 리사이즈 |
| 연필 | `P` | 가는 자유 곡선 |
| 브러시 | `B` | 두꺼운 자유 곡선 (두께 3배) |
| 지우개 | `E` | 픽셀 지우기 |
| 채우기 | `F` | 클릭한 영역을 전경색으로 채우기 |
| 스포이드 | `K` | 클릭한 픽셀의 색을 전경색으로 |
| 텍스트 | `T` | 캔버스에 텍스트 입력 |

### 선 두께

리본의 **두께** 섹션에서 선택하거나 직접 숫자 입력 (1~100px)

---

## 도형 그리기

| 도형 | 설명 |
|---|---|
| 선 | 직선 (드래그) |
| 사각형 | 직사각형 (드래그) |
| 타원 | 원/타원 (드래그) |
| 화살표 | 화살표 방향선 (드래그) |
| 오선 | 악보용 5선 (드래그, 각 선 굵기 = 설정 두께 × 50%) |

**채우기 옵션**: 없음 / 전경색 / 배경색 중 선택

도형을 그린 후 **객체 선택 도구(V)**로 클릭하면 색상·두께·채우기를 변경할 수 있습니다.

---

## 텍스트 입력

### 기본 사용

1. 텍스트 도구(`T`) 선택
2. 캔버스 원하는 위치 클릭 → 입력창 열림
3. 텍스트 입력
4. **Esc** 또는 캔버스 다른 곳 클릭 → 확정

### 텍스트 편집

- **더블클릭** — 기존 텍스트 편집 모드 진입 (객체 목록에서도 가능)
- 편집 중 **Ctrl+Enter** — 확정

### 텍스트 스타일

리본 **텍스트** 섹션에서 설정:

| 옵션 | 설명 |
|---|---|
| 폰트 | 한글/영문 42종 (고딕·명조·손글씨·디스플레이 등) |
| 크기 | 6~300pt |
| **B** | 굵게 |
| **I** | 기울임 |
| **U** | 밑줄 |
| **□** | Boxed Text (박스 테두리) |

### Boxed Text 옵션

**□ 버튼** 활성화 시 추가 설정:

| 옵션 | 설명 |
|---|---|
| 패딩 | 글자와 박스 사이 여백 (px) |
| 반경 | 모서리 둥글기 (0 = 직각) |
| 두께 | 테두리 선 굵기 |
| 배경 | 체크 시 배경색으로 채움 (배경 투명도 적용) |

> 배경색은 텍스트 확정 시점의 **배경색/투명도**가 저장되어 객체마다 독립적으로 유지됩니다.

### 텍스트 입력창 크기 조절

텍스트 입력 중 입력창 **오른쪽 하단 모서리**를 드래그하면 입력창 크기를 조절할 수 있습니다.

---

## 색상 및 팔레트

### 전경색 / 배경색

색상 섹션의 두 색상 박스:
- **위 큰 박스** = 전경색 (선·도형·텍스트 색상)
- **아래 작은 박스** = 배경색 (채우기·Boxed Text 배경)

클릭하면 컬러피커가 열립니다.

### 팔레트 사용

| 동작 | 결과 |
|---|---|
| 스와치 좌클릭 | 전경색 적용 |
| 스와치 우클릭 | 배경색 적용 |

### 커스텀 슬롯 (4개)

자주 쓰는 색상을 저장해두는 슬롯:

| 동작 | 결과 |
|---|---|
| 좌클릭 (색 있을 때) | 전경색으로 적용 |
| 좌클릭 (빈 슬롯) | 컬러피커로 색상 지정 |
| 더블클릭 | 색상 편집 |
| 우클릭 | 배경색으로 적용 |

### 투명도 조절

- **전경 슬라이더** — 선·도형·텍스트의 투명도
- **배경 슬라이더** — 채우기·Boxed Text 배경의 투명도

---

## 객체 선택 및 편집

### 객체 선택 도구 (V)

- **클릭** — 객체 선택 (파란 핸들 표시)
- **드래그** — 이동
- **핸들 드래그** — 크기 조절
- **더블클릭** — 텍스트 편집 모드 진입
- **빈 곳 클릭** — 선택 해제
- **우클릭** — 컨텍스트 메뉴

### 레이어 순서

| 방법 | 동작 |
|---|---|
| Ctrl+] | 위 레이어로 |
| Ctrl+[ | 아래 레이어로 |
| 객체 목록 위↑↓ 버튼 | 레이어 이동 |

### 객체 복사/복제

| 단축키 | 동작 |
|---|---|
| Ctrl+C | 복사 |
| Ctrl+V | 붙여넣기 |
| Ctrl+D | 복제 (제자리) |

### 우클릭 컨텍스트 메뉴

캔버스 우클릭 시:  
복사 · 잘라내기 · 붙여넣기 · 복제 · 전체선택 · 레이어 이동 · 실행취소 · 다시실행 · 삭제

---

## 영역 선택

픽셀 단위 영역을 선택·이동·복사하는 도구 (`R`)

### 사용 순서

1. **드래그** — 점선 사각형으로 영역 지정
2. **영역 안 클릭** — 픽셀 잘라내기 (원본 자리 비워짐)
3. **드래그** — 잘라낸 픽셀 이동
4. **캔버스 클릭** — 현재 위치에 확정

### 영역 선택 조작

| 동작 | 결과 |
|---|---|
| 영역 안 드래그 | 픽셀 이동 |
| **Ctrl + 드래그** | 복사 이동 (원본 유지) |
| Ctrl+C | 복사 (원본 유지) |
| Ctrl+X | 잘라내기 |
| Ctrl+V | 붙여넣기 (캔버스 중앙) |
| Delete | 선택 픽셀 삭제 |
| Esc | 취소 (원위치 복원) |
| 화살표 키 | 1px 이동 |
| Shift+화살표 | 10px 이동 |

---

## 캔버스 크기 조절

### 핸들 드래그

캔버스 테두리의 **회색 원형 핸들**을 드래그:

| 방향 | 동작 |
|---|---|
| 오른쪽 (e) | 캔버스 우측 확장 |
| 아래쪽 (s) | 캔버스 하단 확장 |
| 왼쪽 (w) | 캔버스 좌측 확장 + 객체 오른쪽 이동 |
| 위쪽 (n) | 캔버스 상단 확장 + 객체 아래쪽 이동 |

> 객체 크기와 비율은 변경되지 않습니다.

> **파란 사각형 핸들**은 선택된 객체의 크기 조절 핸들입니다 (캔버스 핸들과 구분).

### 크기 조절 메뉴

파일탭 **크기조절** 버튼:
- 슬라이더 또는 % 직접 입력 (10~400%)
- 프리셋: 25% / 50% / 75% / 100% / 150% / 200%
- 가로·세로 비율 유지

### 직접 입력

보기탭 **배율 조정** 섹션에서 가로·세로 px 직접 입력

---

## 보기 및 테마

### 확대/축소

| 방법 | 동작 |
|---|---|
| `+` / `=` | 확대 |
| `-` | 축소 |
| Ctrl+스크롤 | 확대/축소 |
| 100% 버튼 | 1:1 원본 크기 |

### 테마

보기탭 **테마** 섹션:
- ☀️ **라이트** — 흰 배경 UI
- 🌙 **다크** — 어두운 배경 UI
- 선택한 테마는 자동 저장됩니다

---

## 전체 단축키

### 파일

| Windows | Mac | 기능 |
|---|---|---|
| Ctrl+N | ⌘+N | 새 파일 |
| Ctrl+O | ⌘+O | 열기 (이미지/EPP) |
| Ctrl+S | ⌘+S | 저장 (PNG) |
| Ctrl+Shift+S | ⌘+Shift+S | 다른이름으로 저장 |
| Ctrl+E | ⌘+E | EPP 저장 |
| Ctrl+P | ⌘+P | 인쇄 |

### 편집

| Windows | Mac | 기능 |
|---|---|---|
| Ctrl+Z | ⌘+Z | 실행 취소 |
| Ctrl+Y | ⌘+Shift+Z | 다시 실행 |
| Ctrl+C | ⌘+C | 복사 |
| Ctrl+X | ⌘+X | 잘라내기 |
| Ctrl+V | ⌘+V | 붙여넣기 |
| Ctrl+D | ⌘+D | 복제 |
| Ctrl+A | ⌘+A | 전체 선택 |
| Delete / Backspace | Delete | 삭제 |
| Escape | Escape | 선택 해제 / 취소 |

### 레이어

| Windows | Mac | 기능 |
|---|---|---|
| Ctrl+] | ⌘+] | 위 레이어로 |
| Ctrl+[ | ⌘+[ | 아래 레이어로 |

### 도구 선택

| 단축키 | 도구 |
|---|---|
| R | 영역 선택 |
| V | 객체 선택/이동 |
| P | 연필 |
| B | 브러시 |
| E | 지우개 |
| F | 채우기 |
| K | 스포이드 |
| T | 텍스트 |

### 보기

| Windows | Mac | 기능 |
|---|---|---|
| + 또는 = | + 또는 = | 확대 |
| - | - | 축소 |
| Ctrl+스크롤 | ⌘+스크롤 | 확대/축소 |

### 객체 이동 (선택 후)

| 단축키 | 동작 |
|---|---|
| ↑ ↓ ← → | 1px 이동 |
| Shift+↑↓←→ | 10px 이동 |

### 텍스트 편집 중

| 단축키 | 동작 |
|---|---|
| Escape | 텍스트 확정 |
| Ctrl+Enter | 텍스트 확정 |

---

## 팁

- **작업 중 정기적으로 EPP 저장**을 권장합니다. EPP 파일로 저장하면 모든 객체의 편집 가능한 상태가 보존됩니다.
- **이미지 파일(PNG 등)로 저장**하면 픽셀로 합쳐져 개별 객체 편집이 불가능합니다.
- **객체 목록 패널**에서 텍스트 항목을 더블클릭하면 캔버스에서 찾지 않고 바로 편집할 수 있습니다.
- **드래그앤드롭**으로 이미지를 불러올 때 일반 드롭은 현재 캔버스에 객체로 삽입되고, Shift+드롭은 새 파일로 열립니다.
- **오선 도형**은 악보 작성에 유용하며, 선 굵기 설정의 50%로 각 선이 그려집니다.

---

*Easy Paint Pad · HTML5 Canvas 기반 · 외부 라이브러리 없음*
